@model IEnumerable<Lotofacil.Application.Features.Statistics.TopContestViewModel>
@{
    ViewData["Title"] = "Dashboard";
    var contests = Model.ToList(); // Converte o IEnumerable em uma lista indexável
}

<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Top 2 Concursos com melhor desempenho</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="/Home/Index">Página Inicial</a></li>
                    <li class="breadcrumb-item active">Top 2 Concursos</li>
                </ol>
            </div>
        </div>
    </div>
</section>

<div class="container-fluid d-flex justify-content-center align-items-center" style="min-height: 50vh;">
    <div class="row">
        @for (int i = 0; i < contests.Count; i++)
        {
            <div class="card card-danger card-outline" style="margin:16px">
                <div class="card-header">
                    <h5 class="card-title m-0">@("Top " + (i + 1) + " Concurso ")</h5>
                </div>
                <div class="card-body">
                    <p><strong>Nome:    </strong> @contests[i].Name</p>
                    <p><strong>Data:    </strong> @contests[i].Data.ToShortDateString()</p>
                    <p><strong>Números: </strong> @contests[i].Number</p>
                    <p><strong>Total de Concursos com +11 acertos:  </strong> @contests[i].CountContests</p>
                    <p><strong>Top 10 números mais frequentes:</strong> @contests[i].TopTenNumbers</p>
                    <hr>
                    <ul>
                        @foreach (var occurrence in contests[i].NumberOccurences)
                        {
                            <li>Número: <strong>@occurrence.Number</strong> - Ocorrências: <strong>@occurrence.Occurences</strong></li>
                        }
                    </ul>
                </div>
            </div>
        }
    </div>
</div>